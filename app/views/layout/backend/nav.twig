<nav class="navbar is-dark" id="nav-app-wrap">
    <div class="navbar-brand">
        <a class="navbar-item" href="https://bulma.io">
            <img src="http://destination.com.au/img/DESTINATION_REV_LOGO.png" alt="Destination Events" width="112" height="32">
        </a>
        <div class="navbar-burger burger" data-target="navbarExampleTransparentExample">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>

    <div id="navbarExampleTransparentExample" class="navbar-menu">
        <div class="navbar-start">
            <a class="navbar-item" href="{{ get_route('homepage') }}" target="_blank">
                Home
            </a>
            <a class="navbar-item" href="#">
                Developer's Document
            </a>
            <div class="navbar-item">
                <el-autocomplete
                        v-model="keyword"
                        style="width: 400px;"
                        :fetch-suggestions="querySearchAsync"
                        placeholder="Find a user: Email / First Name"
                        @select="handleSelect"
                        :hide-loading="true"
                        :trigger-on-focus="false"
                >
                    <template slot-scope="{ item }">
                        <p class="name">${ item.firstname } ${ item.lastname } - ${ item.company_name }(${ item.position })</p>
                    </template>
                </el-autocomplete>
            </div>
        </div>

        <div class="navbar-end">
            <div class="navbar-item">
                <div class="field is-grouped">
                    <p class="control">
                        <a href="{{ url('/user/logout') }}" class="button is-link">
                            <i class="fas fa-sign-out-alt"></i>&nbsp;
                            Logout
                        </a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</nav>